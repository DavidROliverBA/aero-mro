# Production builds route Claude calls through the same-origin Pages Function
# (functions/api/ai.ts), so the Anthropic key stays in Cloudflare and never
# reaches the browser. A relative path — no deployment URL is recorded here.
# Local dev has no such function, so `bun run dev` still uses a runtime key.
VITE_AI_PROXY_URL=/api/ai
